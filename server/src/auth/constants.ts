/**
 * JWT key used between the JWT signing and verifying steps.
 * @note This key is only to get the application going, it should never be used in a production application
 */
export const jwtConstants = {
    secret: 'secretKey',
};